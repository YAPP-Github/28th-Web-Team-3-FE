import { BenefitsPageContent } from "./_components/benefits-page-content";

/**
 * 검색 파라미터를 읽지 않아 정적으로 렌더한다. 탭 전환은 클라이언트에서 즉시 처리한다.
 */
export default function BenefitsPage() {
  return <BenefitsPageContent />;
}
