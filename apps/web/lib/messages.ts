/**
 * 화면 여러 곳이 **글자 그대로 똑같이** 쓰는 문구만 모은다. 같은 상황에 다른 안내가 뜨면
 * 사용자는 다른 문제로 읽는다.
 *
 * 주제가 들어간 문장("목표를 불러오지 못했어요…")은 각 화면에 리터럴로 둔다 — 여기로
 * 끌어오면 화면에 뜨는 문구를 코드에서 grep으로 찾을 수 없게 된다.
 */

/** 재시도로 풀리는 실패에만 붙인다. 입력이 잘못돼서 나는 400에 쓰면 영원히 재시도한다. */
const RETRY_HINT = "잠시 후 다시 시도해 주세요.";

/** 저장 실패 기본 문구 — 어느 값이 문제인지 특정할 수 없을 때 쓴다. */
export const SAVE_FAILED_TEXT = `저장하지 못했어요. ${RETRY_HINT}`;
